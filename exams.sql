CREATE TABLE `reg`.`exams` (
    `examname` VARCHAR(255) NOT NULL,
    `dateuploaded` VARCHAR(255),
    `examtr` VARCHAR(255),
    `examtype` VARCHAR(255),
    `examgrade` VARCHAR(255),
    PRIMARY KEY (`examname`)
);