CREATE TABLE `reg`.`assignments` (
    `assignname` VARCHAR(255) NOT NULL,
    `dateuploaded` VARCHAR(255),
    `assigntr` VARCHAR(255),
    `assigntype` VARCHAR(255),
    `assigngrade` VARCHAR(255),
    `assignduedate` VARCHAR(255),
    `assigninstructs` VARCHAR(255),
    `assignfile` VARCHAR(255),
    PRIMARY KEY(`assigname`)
);